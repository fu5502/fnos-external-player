<div align="center">

# 🎬 飞牛影视 (fnOS) 全能外部播放器调用插件

**为飞牛 OS (fnOS) 飞牛影视 (`trim.media`) 打造的服务端免插件注入增强工具**

[![GitHub Release](https://img.shields.io/badge/Release-v4.4-blue.svg?style=flat-square)](https://github.com/fu5502/fnos-external-player/releases)
[![License: MIT](https://img.shields.io/badge/License-MIT-green.svg?style=flat-square)](LICENSE)
[![Platform](https://img.shields.io/badge/Platform-fnOS%20%7C%20Linux%20x86__64-orange.svg?style=flat-square)](https://www.fnnas.com/)
[![Supported Players](https://img.shields.io/badge/Players-PotPlayer%20%7C%20VLC%20%7C%20IINA%20%7C%20Infuse%20%7C%20MPV-purple.svg?style=flat-square)](#-支持的外部播放器矩阵)

[功能特性](#-功能特性) • [效果预览](#-效果预览) • [一键安装](#-一键安装) • [播放器矩阵](#-支持的外部播放器矩阵) • [系统架构](#-核心架构与技术突破) • [常见问题](#-常见问题-faq) • [更新日志](#-更新日志)

</div>

---

## 💡 为什么需要本插件？

飞牛影视官方 Web 播放器受制于浏览器环境限制：
* ❌ 遇到 **4K HDR / 杜比视界 (Dolby Vision) / TrueHD 7.1 / DTS-HD / VC-1** 等高规格原盘格式时，容易触发服务端 CPU 软解转码，导致 NAS 瞬间满载发热甚至卡死；
* ❌ 网页端无法发挥 PC / Mac 独立显卡的强大 GPU 硬件解码、超分辨率 (Super Resolution) 与渲染着色器能力；
* ❌ 对于挂载自 **OpenList / AList / 阿里云盘 / 115 / 天翼云盘** 的 `.strm` 虚拟串流，传统方式可能存在鉴权排队、参数截断或播放失败。

**本插件彻底解决以上痛点！** 客户端零侵入（免浏览器油猴插件/扩展），只需服务端一次性注入，即可在任意浏览器中一键调起本地专业播放器，享受**0% CPU 占用、原汁原味 4K 蓝光画质秒播**体验！

---

## 📸 效果预览

![飞牛影视外部播放器效果预览](docs/preview.png)

> **极简质感**：工具栏自动适配飞牛影视暗黑主题与毛玻璃特效，无缝嵌入电影、电视剧及单集详情页。

---

## 🌟 功能特性

* ⚡ **OpenList / AList 级别极致秒播**：
  * **0 毫秒即时响应**：去除传统点击时的异步网络延迟，点击按钮瞬间（0ms）直接触发系统协议唤起播放器，体验与在 OpenList / AList 网页中秒开完全一致；
  * **0.07 秒极速 302 直连**：全面兼容 Alist / OpenList / 天翼云盘 / 115 / 夸克 / 阿里云盘 生成的 `.strm` 文件，毫秒级 302 重定向到顶级 CDN 直链，跑满千兆宽带秒开；
* 🏷️ **权威真实中文片名识别（`/fnmeta` 毫秒级直通）**：
  * 服务端直连 SQLite 数据库与底层存储，精准提取电影、电视剧、分集及番外篇的原生真实中文文件名；
  * PotPlayer / VLC 播放列表和窗口标题栏 **100% 精准展示纯中文原名**（如 `冷库01：捉迷藏.rmvb`、`誓言.mp4`、`怒之杀 (2026).mp4`），彻底杜绝 `%E9%...` 乱码与占位符；
* 🔥 **多线程并发推流网关（端口 `5668`）**：
  * 专为高规格 4K 原盘设计的多线程并发推流架构，瞬时响应 PotPlayer 针对音轨、字幕、索引发起的 10~20 个并行 Range 探测请求，**0% 转码 CPU 占用，拖动进度条毫秒级响应**；
* ⏱ **历史观影进度断点续播**：
  * 自动同步飞牛影视原生观影历史记录，调起播放器时自动附带 `/seek=hh:mm:ss`，从上次停顿的精确秒数无缝续播；
* 🛡 **系统与应用升级防失效**：
  * 配备 systemd 服务常驻守护与 Cron 定时自愈校验，fnOS 系统更新或飞牛影视升级后自动恢复注入，无需反复手动配置；
* 🖥 **客户端完全免配置**：
  * 纯服务端静态资源无损注入，PC、Mac、手机、平板浏览器访问飞牛影视均可直接呈现外部播放器工具栏。

---

## 🚀 一键安装

SSH 登录你的 **fnOS 终端**（或控制台），以 `sudo` 运行以下命令即可全自动安装：

```bash
curl -sSL https://raw.githubusercontent.com/fu5502/fnos-external-player/main/install.sh | sudo bash
```

> [!TIP]
> 安装完成后，请在浏览器中打开飞牛影视，按 **`Ctrl + F5`**（macOS 为 **`Cmd + Shift + R`**）强制刷新缓存，进入任意影视详情页即可看到外部播放器工具栏！

---

## 🗑️ 一键卸载

如果需要完整卸载并还原官方初始状态，直接在终端执行：

```bash
curl -sSL https://raw.githubusercontent.com/fu5502/fnos-external-player/main/uninstall.sh | sudo bash
```

---

## 🎮 支持的外部播放器矩阵

| 播放器 | Windows | macOS | iOS / iPadOS | Android | 特色说明 |
| :--- | :---: | :---: | :---: | :---: | :--- |
| **PotPlayer** | ✅ | — | — | — | **Windows 平台首选**，完美支持 `/seek` 断点续播与原生中文片名显示 |
| **VLC** | ✅ | ✅ | ✅ | ✅ | **跨平台万能神器**，全平台原生协议一键调起 |
| **IINA** | — | ✅ | — | — | **macOS 顶级现代播放器**，完美融入 macOS 设计语言 |
| **Infuse** | — | ✅ | ✅ | — | **Apple 生态音画标杆**，支持 Apple TV、杜比全景声与空间音频 |
| **MPV** | ✅ | ✅ | ✅ | ✅ | **极客轻量级硬解**，Base64 协议流式直推 |
| **弹弹Play** | ✅ | — | — | ✅ | **二次元追番神器**，全自动匹配动漫网络弹幕同屏播放 |
| **NPlayer** | — | ✅ | ✅ | ✅ | **移动端老牌神器**，支持 DTS-HD / 杜比音效硬解 |
| **恒星播放器** | ✅ | ✅ | ✅ | ✅ | **国产高画质播放器**，支持 AI 插帧增强与 HDR 映射 |
| **复制串流** | ✅ | ✅ | ✅ | ✅ | 一键复制标准 HTTP 206 串流直链，可粘贴至任意播放器或下载器 |

---

## 🛠 核心架构与技术突破

```text
┌────────────────────────────────────────────────────────┐
│                   浏览器 (飞牛影视 Web)                 │
│  • 进入详情页 5ms 静默预请求元数据 (/fnmeta/:guid)       │
│  • 点击外部播放器按钮 0ms 同步即时触发系统协议           │
└──────────────────────────┬─────────────────────────────┘
                           │ potplayer://http://<NAS>:5668/fnplay/<guid>/<真实中文片名>
                           ▼
┌────────────────────────────────────────────────────────┐
│         Direct Stream 多线程网关服务 (:5668)           │
│  • SQLite 毫秒级查询 trimmedia.db 定位底层媒体文件      │
└──────────────┬───────────────────────────┬─────────────┘
               │ 本地原盘 (MP4/MKV/ISO)    │ 网盘虚拟串流 (.strm)
               ▼                           ▼
┌──────────────────────────────┐ ┌────────────────────────────────────────┐
│     HTTP 206 多线程并行直推   │ │  0.07 秒极速 302 重定向至 OpenList     │
│  • 并发无阻塞响应 Range 分块 │ │  • 自动执行 RFC 3986 URL 编码        │
│  • 0% CPU 占用，显卡硬件解码  │ │  • 直连阿里云/115/天翼云顶级 CDN 节点 │
└──────────────┬───────────────┘ └───────────────────┬────────────────────┘
               │                                     │
               ▼                                     ▼
┌─────────────────────────────────────────────────────────────────────────┐
│        本地专业播放器 (PotPlayer / VLC / IINA / Infuse / MPV ...)        │
│  • 播放列表与标题栏 100% 精确显示影视原名（如：冷库01：捉迷藏.rmvb）    │
│  • 4K 蓝光原画无损秒开、音画轨道自由切换、杜比视界高动态呈现            │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## ❓ 常见问题 (FAQ)

### Q1: 点击 PotPlayer 提示“在开始播放时发生了问题，无法播放”？
* **原因**：通常是因为影视文件路径中包含了未编码的中文字符或特殊符号（如 `[` `]`），或者是旧版本单线程服务阻塞。
* **解决**：升级到 **v4.4** 最新版即可彻底解决。本插件已内置 RFC 3986 规范的 URL 安全编码器，可 100% 兼容包含各种复杂中文字符与符号的路径。

### Q2: 为什么我的剧集在播放器中显示为“视频播放”或“首页.mkv”？
* **原因**：部分番外剧集或未刮削剧集在前端 DOM 中未单独渲染标题，旧版本 DOM 抓取可能会误抓导航项。
* **解决**：**v4.4** 引入了服务端直连元数据架构（`/fnmeta`），由服务端直接从 SQLite 数据库和 `.strm` 文件提取权威物理文件名，不再依赖前端 DOM，100% 正确显示。

### Q3: 飞牛影视更新后外部播放器按钮消失了怎么办？
* **无需担心**：本插件已配置 systemd 常驻守护和每小时自愈定时任务。如果飞牛影视升级覆盖了前端文件，守护程序会在检测到时自动重新注入。你也可以随时再次运行 `install.sh` 极速恢复。

---

## 📝 更新日志

### 🚀 v4.4 (最新版本)
* **[新增]** `/fnmeta` 毫秒级元数据直通接口，直接从 SQLite 数据库与 `.strm` 文件读取权威真实中文文件名；
* **[优化]** 前端后台静默预加载机制，点击播放瞬间零延迟带出真实中文片名（如 `冷库01：捉迷藏.rmvb`、`誓言.mp4`）；
* **[修复]** 彻底消除单页面路由切换导致的“首页.mkv”或 GUID 串展示问题。

### ⚡ v4.3 ~ v3.9 (性能飞跃)
* **[重构]** 0 毫秒原生同步唤起机制，对标 OpenList / AList 极致秒开体验；
* **[升级]** 全面采用 `ThreadingHTTPServer` 多线程高并发架构，并行处理高码率原盘的数十个 Range 轨道探测；
* **[优化]** 0.07 秒极速 302 重定向，直通 OpenList 与云端顶级 CDN 直链；
* **[修复]** 加入 RFC 3986 规范的 URL 安全编码器，解决 STRM 路径中括号与中文字符导致的播放器报错。

### 🎬 v3.8 ~ v1.0 (基础建设)
* **[发布]** 独家 Direct Stream 网关服务（端口 5668），免 Token 鉴权与 Cookie 绑定；
* **[支持]** 支持 PotPlayer、VLC、IINA、Infuse、MPV、弹弹Play、NPlayer、恒星播放器全矩阵；
* **[新增]** 历史观影进度断点自动续播 (`/seek`)；
* **[发布]** 一键全自动安装与卸载脚本（`install.sh` / `uninstall.sh`）。

---

## 📄 开源协议

本项目基于 [MIT License](LICENSE) 开源协议分发。欢迎提交 Issue、Fork 与 Pull Request 共建！

<div align="center">
如果这个项目对你有帮助，欢迎在 GitHub 上点一个 ⭐ <b>Star</b> 支持作者！
</div>